#include <Error.h>
#include <Cell.h>

class Layer {
        double bias;
    public:
        Layer(int a[], double b) : cells (), bias (b) {};
        int cells[];

        void Train() {

        }

        void Test() {
            
        }
};